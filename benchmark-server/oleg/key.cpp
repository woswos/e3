#include <iostream>

using std::cout;

int main(int ac, const char * av[])
{
    if ( ac < 2 )
    {
        cout << "Use: gen, enc, dec\n";
        return 0;
    }

    string cmd = av[1];

    if (0) {}

    else if ( cmd == "gen" )
    {
    }
}
